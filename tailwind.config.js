module.exports = {
    /*
        Create this file with:
        npx tailwind init

        You can define customizations
        that override defaults
    */
    theme: {
        container: {
            center: true,
            padding: '2rem'
        }
    },
    variants: {},
    plugins: []
};
